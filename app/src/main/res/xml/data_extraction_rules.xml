<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ backup + device-to-device transfer config.
     User records (Room DB, DataStore) must survive phone upgrades —
     data loss is the #1 churn driver in this category.

     The entitlement cache is excluded from BOTH passes. Device transfer
     matters as much as cloud backup here: it copies the same file to a second
     physical device, which would hand out a second Pro entitlement from one
     purchase. See backup_rules.xml for the full reasoning. -->
<data-extraction-rules>
    <!-- The -wal/-shm exclusions pair with AutoMinderBackupAgent, which
         checkpoints the write-ahead log into autominder.db before either of
         these passes copies anything. See backup_rules.xml for the reasoning. -->
    <cloud-backup>
        <include domain="database" path="." />
        <include domain="file" path="datastore/" />
        <include domain="sharedpref" path="." />
        <exclude domain="database" path="autominder.db-wal" />
        <exclude domain="database" path="autominder.db-shm" />
        <exclude domain="file" path="datastore/entitlement_cache.preferences_pb" />
    </cloud-backup>
    <device-transfer>
        <include domain="database" path="." />
        <include domain="file" path="datastore/" />
        <include domain="sharedpref" path="." />
        <exclude domain="database" path="autominder.db-wal" />
        <exclude domain="database" path="autominder.db-shm" />
        <exclude domain="file" path="datastore/entitlement_cache.preferences_pb" />
    </device-transfer>
</data-extraction-rules>
